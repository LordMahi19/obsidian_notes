write step by step instruction how to implement the phase mentioned in the message. write the plan in a file and name it with phase number and it should be a md file. for example phase_number_plan.md. that file should be in the folder that has the same name as the phase. then follow instruction that you wrote and implement the solutions. if implementation requires python script then write the scripts with in .py file with a reasonable name. any file that is created for that phase should be inside the folder of that phase. run the scripts and fix any errors. add print statements to help you debug. 